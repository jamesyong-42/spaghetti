# small-grok/ — canonical small Grok fixture

See `scripts/generate-grok-fixture.mjs`. Regenerate with:

```bash
node scripts/generate-grok-fixture.mjs --out crates/spaghetti-napi/fixtures/small-grok
```

Includes `events.jsonl` (timestamps) and `signals.json` (session token aggregates).
